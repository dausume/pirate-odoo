# Allow local + docker network with password or trust (adjust as needed)
# For simplest bootstrap: trust locally; tighten later.
local   all             all                                     trust
host    all             all             127.0.0.1/32            trust
host    all             all             0.0.0.0/0               md5
host    all             all             ::/0                    md5